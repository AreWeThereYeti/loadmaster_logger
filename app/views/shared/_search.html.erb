<%= form_tag('/'+params[:controller]+'/search', :method => :get) do %>
  <div class="row">
    <div class="col-md-4">
      <div class="input-group">
        <% if @search_str %>
          <%= text_field :search, '', :class => 'form-control', :value => @search_str %>
        <% else %>
          <%= text_field :search, '', :placeholder => placeholder, :class => 'form-control', :id => 'user-search-field' %>
        <% end %>
        <span class="input-group-addon with-btn"><%= submit_tag "Søg", :class => 'btn btn-info' %></span>
      </div>
    </div>
  </div>
<% end %>

