<%= form_for(@mobile_device) do |f| %>
  <% if @mobile_device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mobile_device.errors.count, "error") %> prohibited this mobile_device from being saved:</h2>

      <ul>
      <% @mobile_device.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <br>
  <br>
  
  <h4>Her kan du oprette en ny mobil enhed for en vognmand. Enheden skal registreres med et ID som du får når du åbner Loadmaster Logger app'en på android tableten. Dette ID skal du indtaste herunder hvorefter der bliver genereret en kode som du skal indtaste på android tableten. </h4>
  
  
  <div class="field">
    <%= f.text_field :device_id, placeholder: "ID"  %> 
    <span class="inline">Åben loadmaster logger app'en på din mobile enhed og indtast det ID den viser dig i dette felt 
  </div>
  <br>
  <br>
  
  <%= f.label 'Vælg venligst den vognmand som du registrerer den mobile enhed for' %><br>
  <%= f.collection_select(:user_id,@haulers, :id, :username, {:class => 'form-control'}) %>
  
  <br>
  <br>
  <br>
  <br>
  
  <% if !@mobile_device[:access_token].nil? %>
    <div class="field">
      <%= f.label :access_token %><br>
      <%= f.text_field :access_token %>
    </div>
  <% end %>
  
  <div class="actions">
    <%= f.submit 'Opret Mobil Enhed', :class => 'btn btn-primary btn-sm' %>
  </div>
  
  <br>
  <br>
<% end %>
